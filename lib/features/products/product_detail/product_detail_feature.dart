import 'package:flutter/material.dart';

class ProductDetailFeature extends StatelessWidget {
  const ProductDetailFeature({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Product detail feature");
  }
}
