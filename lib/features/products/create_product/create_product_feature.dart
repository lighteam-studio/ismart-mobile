import 'package:flutter/material.dart';

class CreateProductFeature extends StatelessWidget {
  const CreateProductFeature({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Create product feature");
  }
}
