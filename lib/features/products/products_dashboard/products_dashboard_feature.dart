import 'package:flutter/material.dart';

class ProductsDashboardFeature extends StatelessWidget {
  const ProductsDashboardFeature({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Products dashboard feature");
  }
}
